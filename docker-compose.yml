version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: file:./dev.db
      NODE_ENV: production
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    restart: unless-stopped
